import { createStore } from 'vuex'

import grcpContainer from './modules/grpc_controller.js'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    grcpContainer
  }
})
